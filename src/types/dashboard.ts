export enum PreferredView {
  PLAYER = 'Player',
  TRAINER = 'Trainer',
  MANAGER = 'Manager',
}

export enum ProfileTab {
  OVERVIEW,
  ACTIVITY,
  EVALUATIONS,
}

export enum EvidenceViewMode {
  INBOUND_EVALUATION,
  OUTBOUND_ACTIVITY,
}

export enum EvidenceType {
  EVALUATED,
  CONNECTED,
}
